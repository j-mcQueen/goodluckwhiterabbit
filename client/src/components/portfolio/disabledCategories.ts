// TEMP: routes listed here are greyed out/non-clickable in the portfolio nav
// (desktop Header + mobile Nav) and redirect to home if visited directly.
// Remove an entry to re-enable that category.
export const DISABLED_CATEGORY_ROUTES: string[] = ["/art"];
